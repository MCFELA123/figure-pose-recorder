# figure-pose-recorder
A small Vite + React + TypeScript web app that records full-body motion from a webcam using **MediaPipe Pose**, then exports that motion as a compact **clip JSON** that the React Native **`StickFigure`** player can replay.  The recorder itself is the web front-end; the rest of the repo (the Expo app) is the consumer..
